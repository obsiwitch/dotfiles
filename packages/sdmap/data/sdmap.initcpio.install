#!/usr/bin/env bash

build() {
    add_module hid-steam
    add_binary sdmap-daemon
    add_binary killall
    add_runscript
}

help() {
    cat <<HELPEOF
The corresponding running hook allows sdmap to be used to type the encryption passphrase.
HELPEOF
}
