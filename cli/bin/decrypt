#!/bin/bash

for encPath in $@; do
    archivePath="$encPath.tar.gz"
    
    gpg --decrypt \
        --output "$archivePath" \
         "$encPath" \
    && \
    aunpack "$archivePath" \
    && \
    rm -rf "$encPath"
    
    rm "$archivePath"
done
