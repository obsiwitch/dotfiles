#!/bin/bash

cp /etc/pacman.d/mirrorlist /etc/pacman.d/mirrorlist.old
curl -s 'https://www.archlinux.org/mirrorlist/?country=FR&protocol=https' \
    | sed 's/#//' \
    | rankmirrors -n 5 - \
    > /etc/pacman.d/mirrorlist
