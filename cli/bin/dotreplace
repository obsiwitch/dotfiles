#!/usr/bin/env python3

# dotreplace - simple string replacement
#
# Synopsis
#   dotreplace string old new
#   dotreplace - old new
#
# Description
#   Simple string replacement (no regex). Print the `string` all occurrences of
#   `old` replaced by `new`. Read standard input if `string` equals '-'.
import sys
if sys.argv[1] == '-':
    sys.argv[1] = sys.stdin.read().rstrip()
print(sys.argv[1].replace(sys.argv[2], sys.argv[3]))
