#!/bin/bash

# dottags-count [path...]
# count all distinct tags/extensions within a directory
# expected format: title[.tag/ext...]

set -o errexit -o nounset

fd --glob '*.*' "$@" \
    | sed 's:.*/::' \
    | grep -o '\.[^.]*' \
    | sort | uniq -c | sort -n
