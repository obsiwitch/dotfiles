#!/usr/bin/env python3

# basic templating
# dottemplate <template_file> [k=v ...] > out

import sys

with open(sys.argv[1]) as f:
    kwargs = dict(kwarg.split('=', maxsplit=1) for kwarg in sys.argv[2:])
    print(f.read().format(**kwargs), end='')
