#!/bin/bash

# Synopsis
#   dottags [path...]
# Description
#   find all distinct tags/extensions within a directory

set -o errexit -o nounset

fd --glob '*.*' "$@" \
    | sed 's:.*/::' \
    | grep -o '\.[^.]*' \
    | sort | uniq -c | sort -n
