#!/bin/bash

set -o errexit -o nounset

# External drive
# * p1 100% · dm-crypt w/ LUKS · ext4: home
backup_extdrive() {
    local out="/run/media/$USER/extyayaka"

    # create borg snapshot before backup
    dotborg snapshot '' 'backup_extdrive'
    dotborg check

    # rsync home
    rsync --archive --delete --protect-args --progress \
        --exclude='.cache' "$HOME" "$out"
}

# Phone SD card
# * p1 100% · no encryption · F32: entertainment & latest borg snapshot
backup_phone() {
    local out="/run/media/$USER/74DB-16F0/"

    # rsync entertainment
    rsync --recursive --delete --protect-args --progress --size-only \
        "$HOME/Downloads" "$HOME/Music" "$HOME/.dotprivate" "$out"

    # copy latest borg snapshot encrypted
    dotborg export-tar --progress "::$(dotborg head)" - \
        | gpg --symmetric --output "$out/borg.tar.enc"
}

help() {
    echo 'usage: dotbackup extdrive'
    echo '       dotbackup phone'
    exit 1
}

[[ -n "${1:-}" ]] || help
if [[ "$1" == "extdrive" ]]; then
    backup_extdrive
elif [[ "$1" == "phone" ]]; then
    backup_phone
else
    help
fi
