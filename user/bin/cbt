#!/bin/bash

set -o errexit -o nounset

# Synopsis
#   cmd | cbt
#   cbt txt
# Description
#   Copy text to clipboard.

if [[ "$#" -eq 0 ]]; then
    cat
else
    echo "$*"
fi | xclip -rmlastnl -selection clipboard
