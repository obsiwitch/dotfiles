#!/bin/sh

i3lock --image=/etc/i3lock/background.png --tiling \
       --ignore-empty-password \
       --no-unlock-indicator

# Unmount plaintext directory when the screen is locked
fusermount -zu $HOME/Private/plaintext

# Prevent files being written in empty plaintext directory
chmod u-w $HOME/Private/plaintext
