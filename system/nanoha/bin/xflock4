#!/bin/sh

i3lock --image=/etc/i3lock/background.png --tiling \
       --ignore-empty-password \
       --no-unlock-indicator

# Unmount Private directory when the screen is locked
fusermount -zu $HOME/Private

# Prevent files being written in empty Private directory
chmod u-w $HOME/Private
