#!/bin/sh

i3lock --image=/etc/i3lock/background.png --tiling \
       --ignore-empty-password \
       --no-unlock-indicator

# Unmount encrypted home when the screen is locked
fusermount -zu /home/$USER/plaintext

# Prevent dotfiles being written in empty home
chmod u-w /home/$USER/main
